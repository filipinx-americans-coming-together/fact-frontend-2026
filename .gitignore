# Next.js
/node_modules
/.next/
/out/
/build
.env*.local
next-env.d.ts
*.tsbuildinfo
.DS_Store

# Raw/original photo backups — not the compressed web copies in public/
*.CR3
*.RW2

# Claude Code local/agent scratch — machine-specific, regenerated per session
.claude/worktrees/
.claude/*.lock

# impeccable local state/history — not shared config
.impeccable/critique/
.impeccable/asset-backups/
.impeccable/hook.cache.json
.impeccable/live/

# graphify caches and dated snapshots — regenerate via `graphify update .`
graphify-out/cache/
graphify-out/cost.json
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/

# vitest / coverage
/coverage
.vercel
.env*
